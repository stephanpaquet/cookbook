### npm 7+, extra double-dash is needed:

```shell
npm create vite@latest my-vue-app -- --template vue

cd my-vue-app

npm install

npm run dev
```
### Commandes

```shell
"dev": "vite",
"build": "vite build",
"preview": "vite preview"
```

